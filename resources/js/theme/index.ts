/**
 * E-Office Theme
 * 
 * Central export for all theme-related tokens and utilities.
 * Import from '@/theme' to access design tokens.
 * 
 * @example
 * import { colors, colorVars } from '@/theme';
 * 
 * // Use hex values directly
 * const style = { color: colors.primary.DEFAULT };
 * 
 * // Use CSS variable references
 * const style = { color: colorVars.primary };
 */

export { colors, colorVars } from './colors';
export type { Colors } from './colors';
